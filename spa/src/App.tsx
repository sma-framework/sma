/**
 * The root of the window. Task 1 stands the build pipeline up; the shell that lives
 * here from now on is assembled in Task 3.
 */
export function App() {
  return <div className="p-8 text-tx">SMA</div>
}
