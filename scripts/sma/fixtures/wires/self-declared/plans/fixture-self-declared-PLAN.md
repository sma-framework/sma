---
phase: wires-fixture
plan: self-declared
type: fixture
must_haves:
  truths:
    - "след, который живёт ТОЛЬКО в собственном объявлении, не найден нигде"
  artifacts:
    - path: "tree/code.txt"
      contains: "WIRE_MARKER_PRESENT_CONTROL"
  key_links:
    - from: "получатель, названный словами, а не путём"
      to: "источник, названный словами, а не путём"
      via: "сузить не до чего — значит ищем по дереву, а каталог планов в дерево не входит"
      pattern: "WIRE_MARKER_SELF_DECLARED"
---

# фикстура описи — СЛЕД ТОЛЬКО В СОБСТВЕННОМ ОБЪЯВЛЕНИИ

У плана ЕСТЬ парная сводка: он закрыт и судим.

`WIRE_MARKER_SELF_DECLARED` встречается ровно в одном месте на свете — в этом самом файле.
Ни один файл дерева его не несёт. Вердикт обязан быть КРАСНЫМ с причиной «след не найден
нигде».

Это замок на самообман прибора. Начни он сканировать каталог планов — и маркер вдруг
«найдётся»: в собственном объявлении, потому что кто-то его сюда напечатал, а не потому что
код его несёт. Прибор поздравил бы сам себя и стал бы зеркалом вместо измерителя.

Запись artifacts здесь зелёная нарочно — контроль против «ничего не нашлось, потому что
зона поиска пуста».
