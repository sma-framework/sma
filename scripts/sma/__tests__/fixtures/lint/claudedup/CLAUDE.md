# Project rules (fixture CLAUDE.md)

Before any push to main run the full test suite and the type check and both must be green.

Never write a pass status to a human gate from a file only the manager may flip those.
