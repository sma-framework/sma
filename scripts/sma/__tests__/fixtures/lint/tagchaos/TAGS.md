# TAGS — closed faceted vocabulary (fixture)

## area

- tech — infrastructure, build, types, migrations, platform engineering.
- common — an overbroad area applied to most of the fixture corpus.
- lonely — an area used by exactly one fixture note.

## kind

- testing — tests, fixtures, vitest, tsc, quality gates. · aliases: tests, ci
- tests — a near-duplicate of testing to prove the near-duplicate-tag check.
- reference — a lookup: addresses, versions, key-fact tables. · aliases: ref, lookup

## phase

- Open facet: `phase:NN` — optional free-form tag.
